{
  "name": "test-todo-app",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/src/**/*.ts": ["prettier --write"],
    "frontend/src/**/*.{ts,tsx}": ["prettier --write"]
  }
}
